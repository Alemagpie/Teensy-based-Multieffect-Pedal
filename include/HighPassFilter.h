#ifndef HPF_H
#define HPF_H

//y(nT) = α*[y((n-1)T)+x(nT)-x((n-1)T)]

// α = 1 - e^(-2*pi*fc/Fs) -> fc = cutoff freq, Fs = sample rate

class HighPassFilter {
    public:
        
    private:

};

#endif